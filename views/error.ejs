<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'Ошибка' %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <style>
        body {
            background-color: #1a1a1a;
            color: #ffffff;
            font-family: 'Poppins', sans-serif;
            text-align: center;
            padding: 50px;
        }
        h1 {
            font-size: 5rem;
            color: #f44336;
            font-weight: bold;
            margin-bottom: 20px;
            animation: fadeInDown 1s;
        }
        h2 {
            font-size: 2rem;
            color: #ff9800;
            margin-bottom: 20px;
            animation: fadeIn 1.5s;
        }
        pre {
            background: #333333;
            color: #e0e0e0;
            padding: 15px;
            border-radius: 8px;
            text-align: left;
            overflow-x: auto;
            animation: fadeIn 2s;
        }
        a {
            text-decoration: none;
            font-weight: bold;
            color: #4caf50;
            padding: 10px 20px;
            border: 2px solid #4caf50;
            border-radius: 30px;
            transition: all 0.3s;
        }
        a:hover {
            background-color: #4caf50;
            color: #ffffff;
            transform: scale(1.1);
        }
        @keyframes fadeInDown {
            from {
                opacity: 0;
                transform: translateY(-50px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }
    </style>
</head>
<body>
    <h1>Ошибка: <%= error.status || 500 %></h1>
    <h2><%= message || 'Что-то пошло не так...' %></h2>
    <pre><%= error.stack || 'Подробности ошибки отсутствуют.' %></pre>
    <a href="/" class="btn">Вернуться на главную</a>
</body>
</html>
